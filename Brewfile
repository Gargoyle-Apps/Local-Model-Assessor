# brew bundle — https://docs.brew.sh/Manpage#bundle-subcommand
# Not for integrations/embed-retrieval-stack: use `docker compose exec postgres psql …`
# Python: requirements.txt (pip). libpq: keg-only, conflicts PostgreSQL (`brew info libpq`); PATH for host psql is manual.
brew "libpq"
# cask "docker"
