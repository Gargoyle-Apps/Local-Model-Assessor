# Config location

**Default Continue config:** `~/.continue/config.yaml`

To keep an untracked local copy in this repo folder, run:  
`cp ~/.continue/config.yaml agent-model-management/continue/config.yaml`  
See [agent-model-management/README.md](../README.md) for details.
